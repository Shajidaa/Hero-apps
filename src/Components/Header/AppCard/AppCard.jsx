import React from 'react';

const AppCard = (app) => {
    const singleApp= app;
    console.log(singleApp);
    
    return (
        <div>
            card
        </div>
    );
};

export default AppCard;